import cssTransitionTransform from 'mobiletouch/utils/css-transition-transform';

module('cssTransitionTransform');

// Replace this with your real tests.
test('it works', function() {
  var result = cssTransitionTransform();
  ok(result);
});
