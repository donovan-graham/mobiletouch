import cssTransition from 'mobiletouch/utils/css-transition';

module('cssTransition');

// Replace this with your real tests.
test('it works', function() {
  var result = cssTransition();
  ok(result);
});
